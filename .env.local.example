# 本機開發時想連本機後端（server/，預設 http://localhost:8080）而不是 .env 裡設定的
# 正式站網址，複製這份成 .env.local（Vite 會自動載入、優先權比 .env 高，且已加進
# .gitignore 不會被 commit 上去）：
#
#   npm run env:local
#
# 要切回打正式站：npm run env:prod（會刪掉 .env.local），不用改 .env 本身。
#
# 注意：這裡只給前端（Vite）用，只有 VITE_ 開頭的變數才會生效。LINE_LOGIN_*／
# LINE_CHANNEL_*／DATABASE_URL 這些後端變數要改 server/.env，不是這裡。
VITE_API_BASE_URL=http://localhost:8080
